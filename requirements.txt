# Core ML stack
torch>=2.1
torchvision>=0.16
timm>=1.0            # provides mobilenet_v3_small, efficientnet_lite0, mobilevit_xxs
numpy>=1.24
scikit-learn>=1.3
Pillow>=10.0

# Config
PyYAML>=6.0

# Sustainability accounting
codecarbon>=2.3       # measured compute energy -> kWh / CO2e

# Raspberry Pi export (scripts/export_for_pi.py) — ONNX conversion + int8 quantization
onnx>=1.15
onnxruntime>=1.16

# Dataset download helper (only needed for scripts/download_plantvillage.py) —
# TensorFlow Datasets needs no account/API token, unlike Kaggle.
tensorflow>=2.13
tensorflow-datasets>=4.9
# Environments that pre-ship an older protobuf (e.g. Colab) can end up with
# tensorflow-metadata's bundled *_pb2.py files (compiled against a newer
# protobuf codegen) paired with an older protobuf runtime that can't load
# them — "gencode X runtime Y" VersionError at import time. Pinning a floor
# here forces pip to upgrade it on install instead of leaving the older one.
protobuf>=6.31.1

# Testing
pytest>=7.4
tqdm>=4.66
